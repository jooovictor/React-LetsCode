import { useState } from "react"
/*
const CountButton = ({children, step}) => {

    const[count, setCount] = useState(0)

    const countPlus = () => {
        console.log(count)
        setCount(step + count)
        
    }

    return (
        <button onClick={countPlus}> {count} </button>
    )
}

export default CountButton */