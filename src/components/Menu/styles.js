import {Button} from 'antd'
import styled from 'styled-components'
import { AiOutlineSearch } from "react-icons/ai";

export const BtnRound = styled(Button).attrs({
    className: 'teste',
    shape: 'round',
    type: 'primary'
})` 
`